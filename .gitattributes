# Store text files with LF line endings in the repository,
# whatever the editor or operating system uses locally
* text=auto eol=lf

# Binary assets: git must never touch their bytes
*.woff2 binary
*.png binary
*.ico binary
